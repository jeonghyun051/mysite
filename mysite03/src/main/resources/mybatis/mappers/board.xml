<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">

	
	<select id="findAll" parameterType="int" resultType="boardvo">
		<![CDATA[
			select b.no, b.title, b.contents, b.reg_date as regDate, b.hit, b.group_no as groupNo, b.order_no as orderNo, b.depth, b.user_no as userNo, u.name
			from board b, user u
			where u.no = b.user_no
			order by group_no desc, order_no asc limit #{page },5
		]]>
	</select>
	
	
</mapper>